paths:
  /api/events/{id}:
    delete:
      tags: [Events]
      summary: "Apaga um evento"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: number
                  example: 2
      responses:
        200:
          description: Sucesso